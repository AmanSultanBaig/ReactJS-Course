import React from 'react';

export default function MyDestructuring(props) {
    const {Message} = props
    return(
        <p>{Message}</p>
    )
}

// Destructuring defination is available in class component
